# 场景图优化

## 目标

批处理变换编辑，一次更新世界变换，避免不必要的层次结构变化。

## 先测量

使用专注的命令，一次比较一个更改。当只有一个二进制文件或示例需要重型功能时，避免全局启用它们。

## 命令或模式

```rust
scene.update_world_transforms();
```

## 实际检查

- 保持仅 CPU crate 轻量级。
- 除非输入发生变化，否则避免每帧重建数据结构。
- 分析时分离加载、注册、更新和渲染成本。
