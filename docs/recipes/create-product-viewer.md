# 创建产品查看器

## 使用场景

当你需要此行为在应用程序中并想要支持它的最小 Scenix 子系统集时。

## 方法

结合 glTF 加载、轨道相机、光线投射器选择、辅助工具和可选的后处理。

## 示例

```toml
scenekit = { version = "1", features = ["loader", "renderer", "post"] }
```

## 验证

围绕上述状态更改或命令添加专注测试。对于浏览器或 GPU 路径，保持测试门控以便正常 CPU CI 保持快速。