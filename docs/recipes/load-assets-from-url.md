# 从 URL 加载资产

## 使用场景

当你需要此行为在应用程序中并想要支持它的最小 Scenix 子系统集时。

## 方法

启用加载器 `http` 功能，并在目标支持时使用 URL 加载远程资产字节。

## 示例

```toml
scenekit-loader = { version = "1", features = ["http"] }
```

## 验证

围绕上述状态更改或命令添加专注测试。对于浏览器或 GPU 路径，保持测试门控以便正常 CPU CI 保持快速。