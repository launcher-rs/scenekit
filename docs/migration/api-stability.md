# API 稳定性

Scenix v1.0.0 将公共 API 视为稳定。未来的发布应优先选择累加更改，在移除之前弃用重复或实验性 API，并记录迁移步骤。

## 稳定表面

稳定表面包括外观功能名称、专注的 crate 边界、CPU 创作模型、显式渲染器资源注册、加载器输出所有权、光线投射器行为、辅助线几何体、Animato 桥接角色和 WASM 包装器范围。

## 弃用策略

已弃用的项目应包含 `since = "1.0.0"` 或引入弃用的版本以及直接替换说明。

## 用户规则

如果你依赖可选系统，请在 Cargo.toml 中保持其功能显式，以便未来的审计简单明了。